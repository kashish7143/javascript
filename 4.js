const obj={
    js:'javascript',
    cpp:'c++',
    rb:'ruby',
    swift:'swift by apple',
}

for(const key in obj){
    //console.log(`${key} shortcut is for ${obj[key]}`)
}

// forin array 

const arr=['a',"b","c",2,3,4,5]

for(const num in arr){

console.log(arr[num])
}